# my-application
# 🔍 AI Image & Text Detector (Streamlit App)

This project is a simple AI detector that identifies whether an **image** is AI-generated or real using the model:

👉 `umm-maybe/AI-image-detector`

## 🚀 Features
- Detect AI vs Real images
- Basic text upload support
- Fast model loading with `@st.cache_resource`
- Fully deployable to Streamlit Cloud

## 📦 Installation (Local)
```bash
pip install -r requirements.txt
streamlit run app.py
